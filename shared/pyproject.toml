[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "onpyrunner-shared"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.8.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/onpyrunner_shared"]

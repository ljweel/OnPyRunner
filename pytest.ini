[pytest]
# 커스텀 마커 등록 (경고 방지 및 문서화)
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')

# 기본 실행 옵션 (선택 사항)
# -ra: 결과 요약 표시, --strict-markers: 등록되지 않은 마커 사용 시 에러 발생
addopts = -ra --strict-markers

# 테스트 탐색 경로 설정 (선택 사항)
testpaths = test
